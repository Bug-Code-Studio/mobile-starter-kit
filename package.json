{
  "name": "mobile-starter-kit",
  "private": true,
  "workspaces": [
    "cli"
  ],
  "scripts": {
    "cli:build": "npm --workspace cli run build",
    "cli:dev": "npm --workspace cli run dev --",
    "cli:test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^5.0.0"
  }
}
